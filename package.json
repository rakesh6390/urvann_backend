{
  "name": "urvaan",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed:data": "node scripts/seedData.js",
    "seed:admin": "node scripts/seedAdmin.js",
    "install-deps": "npm install",
    "build": "echo 'No build step needed for Node.js backend'",
    "render-build": "npm run install-deps && npm run seed:data && npm run seed:admin",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
,
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0",
    "nodemon": "^3.1.10"
  }
}
